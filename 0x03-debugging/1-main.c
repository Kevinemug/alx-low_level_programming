#include <stdio.h>
/**
 * main - causes an infinite loop
 * Return: is 0
 */
int main(void)
{
int i;
printf("Infinite loop incoming :(\n");
i = 0;
/**
 * while - initializing an infinite loop.
 * while (i < 10)
 * {
 * putchar(i);
 * }
 *printf - prints avoided infinite loop message.
 */
printf("Infinite loop avoided! \\o/\n");
return (0);
}

