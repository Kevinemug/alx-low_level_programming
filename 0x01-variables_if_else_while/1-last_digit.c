#include<stdlib.h>
#include<stdio.h>
#include<time.h>
/**
 *
 *  program will assign a random number to the variable n each time it is executed
 */
int main(void)
{
	int n;
	srand(time(0));
	n= rand() - RAND_MAX/2
		if(n % 2 > 5)
			printf("last

	 
